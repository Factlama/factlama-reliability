name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Checkout factlama-architecture (canonical contracts/v0.1 fixtures)
        uses: actions/checkout@v4
        with:
          repository: Factlama/factlama-architecture
          path: factlama-architecture

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package with dev extras
        run: pip install -e ".[dev]"

      - name: Lint (ruff check)
        run: ruff check .

      - name: Format check (ruff format)
        run: ruff format --check .

      - name: Type check (mypy)
        run: mypy schemas core judges

      - name: Dependency-boundary check (import-linter)
        run: lint-imports

      - name: Tests (pytest), including the G0 contract fixture suite
        run: pytest --cov=schemas --cov=core --cov=judges --cov-report=term-missing
        env:
          FACTLAMA_CONTRACTS_DIR: ${{ github.workspace }}/factlama-architecture/contracts/v0.1
