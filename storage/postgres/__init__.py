"""The PostgreSQL `StorageBackend` implementation (ADR-019). Only
`storage/postgres/*` itself and this repo's composition root (PR2/PR3) may
import this package -- see `pyproject.toml`'s import-linter contracts."""
